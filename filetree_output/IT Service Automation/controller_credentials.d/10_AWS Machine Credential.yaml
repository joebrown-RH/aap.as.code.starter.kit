---
controller_credentials:
  - name: AWS Machine Credential
    description: |-
      {%- raw -%}
      ssh key based authentication
      {%- endraw -%}
    credential_type: Machine
    organization: IT Service Automation
    inputs:
      username: ec2-user
      ssh_key_data: '{{ vaulted_controller_credentials_aws_machine_credential_ssh_key_data }}'
      become_method: sudo
...
