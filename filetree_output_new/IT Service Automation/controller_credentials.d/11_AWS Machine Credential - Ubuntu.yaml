---
controller_credentials:
  - name: AWS Machine Credential - Ubuntu
    description: |-
      {%- raw -%}
      ssh key based authentication
      {%- endraw -%}
    credential_type: Machine
    organization: IT Service Automation
    inputs:
      username: ubuntu
      ssh_key_data: '{{ vaulted_controller_credentials_aws_machine_credential___ubuntu_ssh_key_data }}'
      become_method: sudo
...
